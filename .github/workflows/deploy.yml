name: Deploy to Hostinger VPS

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.HOSTINGER_SSH_PRIVATE_KEY }}

      - name: Install dependencies
        run: |
          /usr/bin/php8.2 composer update
          /usr/bin/php8.2 composer install --no-dev --prefer-dist --optimize-autoloader
          npm install
          npm run build

      - name: Deploy to VPS
        run: |
          ssh ${{ secrets.HOSTINGER_SSH_LARAVEL_USER }} << 'EOF'
            cd /htdocs/srv590533.hstgr.cloud
            git pull origin master
            /usr/bin/php8.2 composer install --no-dev --prefer-dist --optimize-autoloader
            npm install
            npm run build
            /usr/bin/php8.2 artisan migrate --force
            /usr/bin/php8.2 artisan cache:clear
            /usr/bin/php8.2 artisan config:cache
            /usr/bin/php8.2 artisan route:cache
            /usr/bin/php8.2 artisan view:cache
          EOF
